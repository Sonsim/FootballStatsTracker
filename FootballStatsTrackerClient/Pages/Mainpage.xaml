<Page x:Class="FootballStatsTrackerClient.Pages.Mainpage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:FootballStatsTrackerClient.Pages"
      xmlns:user="clr-namespace.FootballStatsTrackerClient.Globals"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Background="{Binding homemaincolor}"
      Foreground="{Binding homesecondcolor}"
      Title="Hovedside">
<Grid>
    <Grid.RowDefinitions>
        <RowDefinition Height="*" />
        <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <TextBlock HorizontalAlignment="Center"
               VerticalAlignment="Center"
               Grid.Row="0"
               Text="Hei"/>
    <Button 
            Content="Endre farger" 
            Grid.Row="1"
            Foreground="{Binding awaysecondcolor}"
            Background="{Binding awaymaincolor}"
            Height="35"
            Width="150"
            Margin="0,5,0,0"
            Click="AlterTheme_Click"> </Button>
    </Grid>
</Page>
